# healthAlst client workspace

Deployable Next.js applications live in `apps/`; shared packages live in
`packages/`. Run workspace tasks from this directory with pnpm and Turborepo.

```bash
pnpm install
pnpm dev
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

